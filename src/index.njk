---
layout: base
title: "Clawcode Blog"
---
{% set posts = collections.posts | reverse %}
<section>
  <h2>Latest posts</h2>
  {% if posts | length == 0 %}
    <p>No posts have been published yet. Approved stories will appear here.</p>
  {% else %}
    <ol class="post-list">
      {% for post in posts %}
        <li>
          <article>
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <p class="post-meta">{{ post.date | date("MMMM d, yyyy") }}</p>
            {% if post.data.description %}
              <p>{{ post.data.description }}</p>
            {% endif %}
          </article>
        </li>
      {% endfor %}
    </ol>
  {% endif %}
</section>
